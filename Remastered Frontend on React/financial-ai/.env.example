# MongoDB Atlas Connection URIs
# Replace with your actual MongoDB Atlas connection strings
# If not provided, the application will use localStorage as fallback
MONGODB_INCOME_URI=mongodb+srv://username:password@cluster.mongodb.net/income_db?retryWrites=true&w=majority
MONGODB_EXPENSE_URI=mongodb+srv://username:password@cluster.mongodb.net/expense_db?retryWrites=true&w=majority

# Database Names (Optional - will use defaults if not specified)
MONGODB_INCOME_DB=income_db
MONGODB_EXPENSE_DB=expense_db

# Collection Names (Optional - will use defaults if not specified)
MONGODB_INCOME_COLLECTION=income_records
MONGODB_EXPENSE_COLLECTION=expense_records

# Environment Variables
# Add your environment-specific variables here
